{% macro entity(rendered) %}
  {% if rendered | isError %}
    {% set renderError %}<!-- Error rendering component: {{ error.message }} -->{% endset %}
    {{ renderError | escape }}
  {% else %}
    {{ rendered | escape }}
  {% endif %}
{% endmacro %}
