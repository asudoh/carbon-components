<button aria-label="Toggle Side Navigation" class="side-nav__toggle-btn" data-interior-left-nav-collapse>
  <div><span class="line"></span><span class="line"></span><span class="line"></span></div>
</button>
<nav role="navigation" aria-label="Interior Left Navigation" data-interior-left-nav class="demo--nav bx--interior-left-nav bx--interior-left-nav--collapseable">
  {% for navPartial in frctl.theme.get('nav') %}
    {% include 'partials/navigation/' + navPartial + '.nunj' %}
  {% endfor %}
</nav>
