imports:
  - ./_loading.yml
namespaces:
  - "{!name}"
  - "class"
props:
  base: "{!prefix}--{!name}"
  svg: "{!prefix}--{!name}__svg"
  stop: "{!prefix}--{!name}--stop"
  small: "{!prefix}--{!name}--small"
  overlay: "{!prefix}--{!name}-overlay"
  overlay-stop: "{!prefix}--{!name}-overlay--stop"
