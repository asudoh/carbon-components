imports:
  - ./_accordion.yml
  - ./accordion--classes.yml
namespaces:
  - "{!name}"
  - "selector"
props:
  init: "[data-{!name}]"
  item: ".{!item}"
  heading: ".{!heading}"
  content: ".{!content}"
